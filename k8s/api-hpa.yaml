apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cms-sync-api
  namespace: cms-sync
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cms-sync-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
