apiVersion: apps/v1
kind: Deployment
metadata:
  name: cms-sync-api
  namespace: cms-sync
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cms-sync-api
  template:
    metadata:
      labels:
        app: cms-sync-api
    spec:
      containers:
        - name: api
          image: ghcr.io/OWNER/REPO:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: cms-sync-config
            - secretRef:
                name: cms-sync-secrets
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
